projects:
- name: fetchy-cha-agents-workshop-queue
  environments:
  - name: production
    databases:
    - name: fetchy-cha-queue-agents-db
      plan: basic-256mb
      region: oregon
      postgresMajorVersion: '18'
    services:
    - type: keyvalue
      name: fetchy-cha-queue-agents-valkey
      plan: starter
      region: oregon
      maxmemoryPolicy: noeviction
      ipAllowList: []
    - type: web
      name: fetchy-cha-queue-agents-web
      runtime: python
      region: oregon
      plan: starter
      buildCommand: pip install uv && uv sync --frozen --no-dev --package workshop-queue-agents
      startCommand: uv run python -m queue_agents.web
      healthCheckPath: /healthz
      envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: fetchy-cha-queue-agents-db
          property: connectionString
      - key: VALKEY_URL
        fromService:
          name: fetchy-cha-queue-agents-valkey
          type: keyvalue
          property: connectionString
      - key: PYTHON_VERSION
        value: '3.12'
    - type: worker
      name: fetchy-cha-queue-agents-worker
      runtime: python
      region: oregon
      plan: starter
      numInstances: 1
      buildCommand: pip install uv && uv sync --frozen --no-dev --package workshop-queue-agents
      startCommand: uv run python -m queue_agents.worker
      envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: fetchy-cha-queue-agents-db
          property: connectionString
      - key: VALKEY_URL
        fromService:
          name: fetchy-cha-queue-agents-valkey
          type: keyvalue
          property: connectionString
      - key: PYTHON_VERSION
        value: '3.12'
